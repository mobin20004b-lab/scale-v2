# scale-v2

ESP32-focused workspace with:

- `esp32/frontend`: PrimeVue-based web control center for settings, capabilities and telemetry.
- `esp32/firmware`: Firmware-side configuration model and API contract stubs.
